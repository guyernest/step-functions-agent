# Direct dependencies only
# Use `uv pip compile requirements.in -o requirements.txt` to generate requirements.txt

# Browser automation engines
nova-act>=0.1.0
openai>=1.0.0  # Required for OpenAI Playwright executor

# AWS & Browser
boto3>=1.28.0
playwright>=1.40.0

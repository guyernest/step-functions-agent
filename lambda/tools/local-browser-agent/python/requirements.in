# Direct dependencies only
# Use `uv pip compile requirements.in -o requirements.txt` to generate requirements.txt

# Browser automation engines (dual-library support for gradual migration)
nova-act>=0.1.0
git+https://github.com/ai-on-cloud/browser-agent.git@main

# AWS & Browser
boto3>=1.28.0
playwright>=1.40.0

{
  "name": "Simple Test",
  "description": "Simple test to verify Nova Act is working - just loads Google",
  "starting_page": "https://www.google.com",
  "abort_on_error": false,
  "steps": [
    {
      "action": "act_with_schema",
      "prompt": "What is the title of this page?",
      "schema": {
        "type": "object",
        "properties": {
          "title": {"type": "string"}
        },
        "required": ["title"]
      },
      "description": "Get page title"
    }
  ]
}

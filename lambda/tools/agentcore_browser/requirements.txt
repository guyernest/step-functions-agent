# Lambda function requirements (minimal - no bedrock-agentcore or nova-act needed)
# The Lambda only routes requests to AgentCore runtime, it doesn't execute agents itself
boto3>=1.35.0
aws-lambda-powertools>=2.0.0
aws-xray-sdk>=2.12.0

# General
.DS_Store
.env
*.log

# Python
**/__pycache__/
*.py[oc]
**/build/
**/dist/
**/wheels/
**/*.egg-info/
**/.venv/
**/.pytest_cache/

# Node.js
**/node_modules/
**/package-lock.json
**/yarn.lock
**/pnpm-lock.yaml

# Rust / Cargo
**/target/
**/Cargo.lock

# AWS SAM
**/.aws-sam/
**/samconfig.toml

# CDK
**/cdk.out/

# UV cache / lock files
# Note: Ignoring lock files like Cargo.lock, package-lock.json, uv.lock
# means builds may not be perfectly reproducible across environments if dependencies
# are not pinned in manifest files (e.g., Cargo.toml, package.json).
# This change is based on user feedback.
**/.uv/
**/uv.lock

# VSCode
**/.vscode/

# Specific project artifacts (if not covered above)
# Example: lambda/tools/web-scraper/layers/chromium/chromium.zip
# Add any other specific large build artifacts or custom output directories here if needed.
